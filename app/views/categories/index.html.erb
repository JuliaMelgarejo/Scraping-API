<p style="color: green"><%= notice %></p>

<h1>Categories</h1>

<div id="categories">
  <% @categories.each do |category| %>
    <%= render category %>
    <p>
      <%# <%= link_to "Show this category", category %> 
    </p>
  <% end %>
</div>

<% if user_signed_in?%>
 <%= link_to "Panel Users", users_panel_path if current_user.admin? %>
 <%= link_to "Panel Categories", categories_panel_path if current_user.admin?  %>
<% end %>

<%= link_to "Cerrar sesión", destroy_user_session_path, data: { turbo_method: :delete} , class: 'btn btn-danger' %>