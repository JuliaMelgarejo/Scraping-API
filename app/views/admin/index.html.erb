<h1>Admin#index</h1>
<p>Find me in app/views/admin/index.html.erb</p>

<%# <% form_with(model: @user, url: admin_index_path, method: :post) do |f| %> 
    <%# <%= f.email_field :email, placeholder: "Email" %>
    <%# <%= f.select :role, User.roles.keys.map { |w| [w.humanize, w] }, include_blank: "Select a role" %> 
    <%# <br /> %>
    <%# <%= f.password_field :password, placeholder: "Password" %>
    <%# <br /> %>
    <%# <%= f.submit %> 
<%# <% end %> 


<% @users.each do |user| %>
  <%= form_for(user, url: admin_path(user), method: :patch) do |f| %>
    <p>
      <%= f.text_field :email %> -
      <%= f.select(:role, User.roles.keys.map { |w| [w.humanize, w] }) %> -
      <%= f.submit "Update" %>
    </p>
  <% end %>

  <p>
     <%= button_to "Destroy this user", admin_path(user), method: :delete %>
  </p>
<% end %>


